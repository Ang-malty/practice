# git init <- 깃 저장소 시작
# git add 파일이름 <- commit 전에 어떤 파일 저장할지 정하기 (로컬)
# git commit -m 커밋내용 <- commit = 깃에 저장사항을 올린거 (로컬)
# git push main origin <- 깃헙에 올리기
# git log = 히스토리
# git status = commit 전에 체크
